{
    "name": "centri-ai-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "pnpm -r run dev",
        "build": "pnpm -r run build",
        "lint": "pnpm -r run lint",
        "test": "pnpm -r run test",
        "db:up": "docker compose up -d",
        "db:down": "docker compose down"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "prettier": "^3.0.0",
        "eslint": "^8.0.0",
        "@types/node": "^20.0.0"
    },
    "engines": {
        "node": ">=18"
    }
}